# Web UI (optional): pip install -r requirements.txt && uvicorn webapp.main:app --reload
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Optional: scripts/tsp_cron_service.py (long-running APScheduler)
apscheduler>=3.10.4
